name: ortools-sat
version: 9.0.0

authors:
  - Abdulla Bubshait <darkstego@gmail.com>

dependencies:
  protobuf:
    github: jeromegn/protobuf.cr

scripts:
  postinstall: cd src && g++ -c cp_sat_wrapper.cpp -o cp_sat_wrapper.o
  
crystal: '>= 1.13.0'

license: MIT
